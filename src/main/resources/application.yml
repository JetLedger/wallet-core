spring:
  datasource:
    url: jdbc:h2:mem:walletdb
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        format_sql: true

server:
  port: 8080

idempotency:
  ttl: PT24H
  redis:
    enabled: true
